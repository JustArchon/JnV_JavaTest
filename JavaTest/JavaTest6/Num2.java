package JavaTest6;

public class Num2 {}

/*
문제: Which two statements are true about localizing an application?
애플리케이션의 지역화(Localization)에 대해 다음 중 두 가지 옳은 설명은 무엇인가?

A) Support for new regional languages does not require recompilations of the code.
새로운 지역 언어에 대한 지원은 코드를 다시 컴파일하지 않고도 가능하다.
B) Textual elements (messages and GUI labels) are hard-coded in the code
텍스트 요소(메시지 및 GUI 레이블)는 코드에 하드코딩되어 있다.
C) Language and region-specific programs are created using localized data.
언어 및 지역에 특화된 프로그램은 지역화된 데이터를 사용하여 생성된다.
D) Resource bundle files include data and currency information
리소스 번들(Resource Bundle) 파일에는 데이터 및 통화 정보가 포함되어 있다.
E) Language codes use lowercase letters and region codes use uppercase letters.
언어 코드는 소문자를 사용하고, 지역 코드는 대문자를 사용한다.

선택한 오답: C) Language and region-specific programs are created using localized data.
		  E) Language codes use lowercase letters and region codes use uppercase letters.
정답: A) Support for new regional languages does not require recompilations of the code.
	 E) Language codes use lowercase letters and region codes use uppercase letters.

이유: B)가 오답인 이유는 텍스트 요소는 properties에 저장되며 하드코드 되지 않습니다.
C)가 틀린 이유는 같은 코드에서 리소스를 바꿔서 지역화하기때문에 지역화된 데이터가 따로 있지않다고합니다.
D)가 틀린 이유는 리소스 번들은 문자열 중심 이며, 통화 정보등의 특수한 정보는 별도로 처리한다고합니다.

*/
