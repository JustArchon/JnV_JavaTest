package JavaTest3;

public class Num12 {}

/*
문제: Which two statements are true about the modular JDK? (Choose two)
모듈화된 JDK에 대해 옳은 설명 두 가지를 고르세요.
	 
A) The foundational APIs of the Java SE Platform are found in the java.base module.
Java SE 플랫폼의 기본 API들은 java.base 모듈에 포함되어 있다
B) An application must be structured as modules in order to run on the modular JDK.
애플리케이션은 모듈로 구성되어야만 모듈화된 JDK에서 실행할 수 있다.
C) It is possible but undesirable to configure modules' exports from the command line
명령줄에서 모듈의 export를 설정할 수는 있지만, 권장되지 않는다.
D) APIs are deprecated more aggressively because the JDK has been modularized
JDK가 모듈화되었기 때문에 API들이 더 공격적으로 deprecated(사용 중단)된다.

선택한 오답: A) The foundational APIs of the Java SE Platform are found in the java.base module.
		  D) APIs are deprecated more aggressively because the JDK has been modularized
정답: A) The foundational APIs of the Java SE Platform are found in the java.base module.
  	 C) It is possible but undersirable to confiure modules' exports from the command line

이유: A) JDK는 자바 베이스 모듈이다.
	 C) 명령줄에서 모듈의 expert설정을 할수 있지만 권장되지는 않는다
	 가 정답입니다.
deprecated = 사용 중단
*/
